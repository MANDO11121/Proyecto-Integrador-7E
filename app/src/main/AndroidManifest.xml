<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.armando.pi">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu_Principal" />
        <activity android:name=".Lista_Clientes" />
        <activity android:name=".Registrar_Usuarios" />
        <activity android:name=".Info_Clientes" />
        <activity android:name=".Modificar" />
        <activity android:name=".Hist_Cliente" />
        <activity android:name=".Registrar_Vendedores"></activity>
    </application>

</manifest>